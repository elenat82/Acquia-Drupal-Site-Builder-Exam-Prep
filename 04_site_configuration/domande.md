# Possibili domande
> 01 Which configuration setting would you adjust to make a Drupal site temporarily inaccessible to the public for maintenance?
- A) Disable all user roles except Administrator
- B) Use Maintenance mode under Configuration --> Development ✅
- C) Remove anonymous permissions
- D) Uninstall the front-end theme


> 02 How do you compare configuration changes from development environment with the active configuration on production environment before import?
- A) You cannot see a list of differences between uploaded and manual configurations
- B) Use diff module to view the differences
- C) Upload the changed configuration file to sync directory via import screen on the production environment. Drupal will show the differences between uploaded and active confguration ✅
- D) Make a list of changes. Compare the configurations manually with the production environment


> 03 Your client copies HTML from an external source and pastes into CKEditor. The client has come to you complaining of broken HTML being displayed on the site. The CKEditor is configured to use 'Full HTML' text format allowing all HTML elements and should continue to allow all elements. How would ou fix the issue of broken HTML?
- A) Enable "Correct faulty and chopped off HTML" filter on Full HTML text format
- B) Suggest using an online HTML validator before pasting into CKEditor
- C) Use JavaScript HTML corrector plugin in your CKEditor
- D) Enable "Limit allowed HTML tags and correct faulty HTML" option on Full HTML ✅